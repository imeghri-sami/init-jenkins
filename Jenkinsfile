pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'Building ...'
            }
        }
        stage('test') {
            steps {
                sh 'Testing ...'
            }
        }
    }
}
